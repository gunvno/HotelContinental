server:
  port: 8086
  servlet:
    context-path: /promotion

spring:
  application:
    name: promotion-service


  datasource:
    url: jdbc:mysql://localhost:3306/promotion_service
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

app:
  services:
    identity: http://localhost:8080/identity
